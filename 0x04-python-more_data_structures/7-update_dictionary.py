#!/usr/bin/python3
# function that replaces or adds key/value in a dictionary

def update_dictionary(a_dictionary, key, value):
    """Replace or add key/value pair in a dictionary."""
    a_dictionary[key] = value
    return (a_dictionary)
